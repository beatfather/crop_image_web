import type { ChatMessage, ChatMessageParams, OpenAIStreamingParams } from './types';
export declare const useChatCompletion: (apiParams: OpenAIStreamingParams) => {
    messages: ChatMessage[];
    loading: boolean;
    submitPrompt: (newMessages?: ChatMessageParams[]) => Promise<void>;
    abortResponse: () => void;
    resetMessages: () => void;
    setMessages: (newMessages: ChatMessageParams[]) => void;
};
